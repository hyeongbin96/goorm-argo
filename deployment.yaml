apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: flaskdemo
  name: flaskdemo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flaskdemo
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: flaskdemo
    spec:
      containers:
      - image: hbkw1772/goorm-project:40
        name: flaskdemo
        volumeMounts:
        - name: flask-db
          mountPath: /web
        resources: {}
      volumes:
      - name: flask-db
        hostPath:
          path: /home/hyeongbin/goorm-jenkins
          type: DirectoryOrCreate
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    app: lb-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: flaskdemo
